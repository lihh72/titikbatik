# Server-side address of the FastAPI backend. Do not add NEXT_PUBLIC_ here.
INTERNAL_API_URL=http://127.0.0.1:8000

# Administrator account for the private Next.js area.
ADMIN_EMAIL=admin@titikbatik.local
ADMIN_PASSWORD=replace-with-a-strong-password
ADMIN_SESSION_TOKEN=replace-with-a-long-random-session-token

# Used by the current Next.js backend adapter. Align this with the automation
# admin authentication contract when the web-to-automation integration is added.
ADMIN_API_TOKEN=replace-with-a-long-random-api-token

# Keep true only for local interface testing without FastAPI.
ALLOW_LOCAL_DEMO=true
